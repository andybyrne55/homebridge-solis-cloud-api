{
  "pluginAlias": "SolisCloudAPI",
  "pluginType": "platform",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "title": "Name",
        "default": "Solis Cloud API",
        "required": true
      },
      "apiKey": {
        "type": "string",
        "title": "API Key",
        "description": "The apiKey and apiSecret can be obtained by following this guide from Solis: https://solis-service.solisinverters.com/en/support/solutions/articles/44002212561-request-api-access-soliscloud",
        "required": true
      },
      "apiSecret": {
        "type": "string",
        "title": "API Secret",
        "required": true
      },
      "deviceId": {
        "type": "string",
        "title": "Device ID",
        "description": "Once you are granted access to the Solis Cloud API, the deviceId can be obtained by running this script with your apiKey and apiSecret: https://github.com/andybyrne55/homebridge-solis-cloud-api/blob/main/solis_cloud_api-get_device_id.sh",
        "required": true
      },
      "apiInterval": {
        "type": "integer",
        "title": "API Poll Interval (seconds)",
        "description": "How often to query the Solis Cloud API",
        "default": 300,
        "minimum": 60
      }
    }
  }
}